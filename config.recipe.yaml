# encoding: utf-8
---
recipe:
  Rx: config
  description: >-
    Append the bilingual hint filter, switch, and settings to one schema.

install_files: >-
  integrations/*/integration.yaml

patch_files:
  ${schema:?schema_required}.custom.yaml:
    - patch/+:
        __include: integrations/${schema:?schema_required}/integration:/patch