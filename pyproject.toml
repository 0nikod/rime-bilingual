[project]
name = "rime-bilingual"
version = "0.1.0"
description = "Generic Rime bilingual candidate comment filter and dictionary builder"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "0nikod" }]
license = { file = "LICENSE" }
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
